monai>=1.3
# Install the appropriate torch build for your platform from https://pytorch.org/get-started/locally/
# For CPU-only you can let pip resolve; for CUDA pick the matching wheel.
torch>=2.2
matplotlib>=3.7
scikit-image>=0.21
